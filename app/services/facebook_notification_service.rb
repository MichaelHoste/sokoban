module FacebookNotificationService
  # Facebook notification to user on facebook if user not active since x days
  def self.delayed_send_notification
  end

  def self.send_notofication(repeat = false)
  end
end
