<!DOCTYPE html>
<html lang="en" prefix="fb: http://www.facebook.com/2008/fbml">
  <head prefix="og: http://ogp.me/ns# fb: http://ogp.me/ns/fb# sokoban_game: http://ogp.me/ns/fb/sokoban_game#">
    <title><%= @level ? "Sokoban - #{@level.pack.name} - #{@level.name}" : "Sokoban - " + _("Free and Social Puzzle Game") %></title>
    <meta charset="UTF-8" />
    <meta name="description" content="<%= @level ? "#{@level.pack.description} (#{@level.pack.file_name}.slc - #{@level.pack.copyright} - #{@level.pack.email} - #{@level.pack.url})" : "Sokoban is a puzzle-game in which the player pushes boxes around in a warehouse, trying to get them to storage locations." %>" />
    <meta name="author" content="Michaël Hoste">
    <meta name="keywords" content="<%= _("sokoban, pushes, moves, brag, puzzle, game, levels, packs, challenge, friends, social") %>" />
    <link rel="shortcut icon" href="/favicon.gif">

    <% if @level %>
      <meta property="fb:app_id"               content="<%= ENV['FACEBOOK_APP_ID'] %>" />
      <meta property="og:type"                 content="sokoban_game:level" />
      <meta property="og:url"                  content="<%= pack_level_url(@level.pack.name, @level.name)%>" />
      <meta property="og:title"                content="<%= @level.name %>" />
      <meta property="og:description"          content="Pack : <%= @level.pack.name.gsub(/\n/," ").gsub(/\r/," ") %> | <%= @level.pack.description.gsub(/\n/," ").gsub(/\r/," ") %>" />
      <meta property="og:image"                content="<%= @level.thumb %>" />
      <meta property="sokoban_game:level_name" content="<%= @level.name %>" />
      <meta property="sokoban_game:pack_name"  content="<%= @level.pack.name %>" />
    <% end %>

    <%= stylesheet_link_tag "application", :media => "all" %>

    <link href='<%= request.protocol %>fonts.googleapis.com/css?family=Droid+Sans:regular,bold' rel='stylesheet' type='text/css' />
    <link href="<%= request.protocol %>fonts.googleapis.com/css?family=Ubuntu:700" rel="stylesheet" type="text/css">

    <%= javascript_include_tag 'application_start' %>
    <%= javascript_include_tag 'jax' %>
    <%#= javascript_include_tag 'jax/runtime' %>
    <%#= javascript_include_tag 'jax/controller-list' %>
    <%= javascript_include_tag 'application_end' %>

    <%= csrf_meta_tags %>

    <%= render 'layouts/google_analytics_headers' %>
    <%= render 'layouts/advert_head' %>
  </head>

  <% new_user = (not cookies[:already_read_the_welcome_message]) ? '1' : '0' %>
  <body data-new-user="<%= new_user %>">
    <%= render 'layouts/facebook_headers' %>
    <%# render 'layouts/googleplus_headers' %>
    <%= render 'layouts/advert_body' %>

    <%# render 'layouts/github_banner' %>
    <%= render "layouts/header" %>
    <%= render "layouts/banner" %>

    <div id="content">
      <%= yield %>
    </div>

    <footer class="footer">
      <%= render 'layouts/footer' %>
    </footer>
  </body>
</html>
