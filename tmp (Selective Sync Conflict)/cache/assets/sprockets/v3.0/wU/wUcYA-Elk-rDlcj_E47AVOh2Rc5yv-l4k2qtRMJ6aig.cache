I"q(function() {
  $(function() {
    var History;
    History = window.History;
    return window.push_this_state = function() {
      var level_real_name, level_slug, pack_real_name, pack_slug;
      pack_real_name = $('#packs').attr('data-pack-name');
      level_real_name = $('#levels').find('.is-selected').attr('data-level-name');
      pack_slug = $('#packs').attr('data-pack-slug');
      level_slug = $('#levels').find('.is-selected').attr('data-level-slug');
      return History.pushState(null, pack_real_name + " - " + level_real_name, "/packs/" + pack_slug + "/levels/" + level_slug);
    };
  });

}).call(this);
:ET